interface:
  display_name: "灵邀AI"
  short_description: "灵邀AI销售 CRM：线索、客户、联系人、机会与跟进管理。"
  default_prompt: "使用 $limob-ai-skill（灵邀AI）时，用户自然聊天也要自动落 CRM：先搜索去重，再新增或更新；联系人按微信号/手机号/姓名顺序识别唯一身份，禁止兼容兜底写法。"

policy:
  allow_implicit_invocation: true
