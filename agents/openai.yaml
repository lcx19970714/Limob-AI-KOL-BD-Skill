interface:
  display_name: "Limob AI KOL BD Skill"
  short_description: "SOP-driven sales ops with outreach, pipeline, and CRM tracking."
  default_prompt: "Use $limob-ai-kol-bd-skill to qualify leads, update pipeline records, and review pipeline status."

policy:
  allow_implicit_invocation: true
